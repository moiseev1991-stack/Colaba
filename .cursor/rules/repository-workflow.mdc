---
name: Repository Workflow Rules
description: Правила работы с Git репозиторием - НЕ ДЕЛАТЬ АВТОМАТИЧЕСКИЙ PUSH
---

# Правила работы с репозиторием Colaba

## ⚠️ КРИТИЧЕСКИ ВАЖНОЕ ПРАВИЛО

**НИКОГДА не выполнять `git push` автоматически!**

Push в удаленный репозиторий выполняется **ТОЛЬКО** после явного указания пользователя.

## Основные правила

1. **Коммиты** - можно делать автоматически после завершения работы
2. **Push** - НИКОГДА автоматически, только по команде пользователя

## Процесс работы

1. Выполнить задачу
2. Добавить изменения: `git add`
3. Создать коммит: `git commit -m "..."`
4. **ОСТАНОВИТЬСЯ** - сообщить пользователю
5. Спросить: "Выполнить push в репозиторий?"
6. Дождаться явного указания пользователя
7. Только после команды выполнить `git push`

## Структура проекта

- Все скрипты → `scripts/`
- Вся документация → `docs/`
- Backend код → `backend/`
- Frontend код → `frontend/`
- Корень - только конфигурационные файлы

**См. подробности**: `docs/guides/PROJECT_STRUCTURE_RULES.md`

## Полные правила

См. `docs/guides/REPOSITORY_WORKFLOW_RULES.md` для полного описания всех правил работы с репозиторием.
