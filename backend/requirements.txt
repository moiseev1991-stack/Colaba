# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
# ollama требует pydantic>=2.9 (см. ResolutionImpossible в docker build)
pydantic>=2.9,<3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
pgvector==0.4.0  # SQLAlchemy adapter для типа VECTOR (модуль maps/reviews_ai)

# Reviews AI (модуль reviews_ai)
hdbscan>=0.8.43  # 0.8.40 несовместим с новой scikit-learn (исчез force_all_finite)
numpy>=1.24

# Admin Panel
sqladmin>=0.19.0
babel>=2.14.0

# Redis & Queue
redis==5.0.1
celery==5.3.4

# Rate Limiting
slowapi>=0.1.9

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
email-validator==2.1.0

# OAuth
fastapi-sso>=0.21.0

# HTTP Client (>=0.27 для yandex-cloud-ml-sdk).
# Верхний bound <0.28 — иначе httpx 0.28 убрал параметр `proxies=` у
# AsyncClient, а openai 1.55 и anthropic 0.39 его ещё передают.
# Симптом: KP /outreach/kp/generate возвращает 422, в логах backend —
# "generate_kp: first/retry chat() failed: AsyncClient.__init__() got
# an unexpected keyword argument 'proxies'".
httpx>=0.27,<0.28
aiosmtplib>=3.0.0
aiohttp==3.9.1
beautifulsoup4==4.12.2
# Playwright — headless Chromium для HTML-парсера 2GIS. 2gis.ru/firm/{id} это SPA,
# чистый GET отдаёт пустой shell без контактов — нужен реальный JS-рендер.
# Системные deps Chromium ставятся в Dockerfile через `playwright install --with-deps chromium`.
playwright>=1.40
duckduckgo-search==4.1.1  # Бесплатный поиск DuckDuckGo без API ключа
yandex-cloud-ml-sdk>=0.17.0  # Yandex Cloud Search API (folder_id + API-ключ)

# LLM & AI
# openai 1.55+ нужен для совместимости с httpx>=0.28 (1.6.1 передавал убранный
# параметр `proxies` в httpx.AsyncClient → AsyncClient.__init__ got an
# unexpected keyword argument 'proxies').
openai>=1.55.0,<2
ollama>=0.4.0
anthropic==0.39.0
google-generativeai==0.8.3

# Excel-экспорт website-лидов (блок 4 ТЗ 2026-06-02). .xlsx с двумя
# вкладками: «Лиды» (для продаж) + «Производство сайта» (для верстальщика).
openpyxl>=3.1.0,<4

# Sentry — error tracking (опционально, активируется при наличии SENTRY_DSN).
# Без DSN init no-op'ит и ничего не отправляет.
sentry-sdk[fastapi]>=1.40,<3

# Starlette SessionMiddleware (signed cookies). Нужно для SQLAdmin auth.
itsdangerous>=2.1

