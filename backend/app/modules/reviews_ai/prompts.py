"""Промпты для LLM-вызовов модуля reviews_ai.

Используются в backend/app/modules/reviews_ai/llm.py. Не хардкодим в коде
вызывающих функций, чтобы можно было легко поправить тон/формат вывода
без правки логики.
"""

SENTIMENT_PROMPT = """Определи тональность каждого отзыва из списка.

Верни ТОЛЬКО JSON-массив, без пояснений и markdown-обёрток:
[{{"id": <review_id>, "sentiment": "positive|negative|neutral", "score": <0.0-1.0>}}, ...]

score — уверенность модели в классификации (1.0 — полная уверенность).

Отзывы:
{reviews_json}
"""

CLUSTER_NAMING_PROMPT = """Тебе дано {count} отзывов клиентов о бизнесах в нише "{niche}".
Эти отзывы попали в один кластер по семантической близости.

Определи общую тему отзывов одним коротким label (2-5 слов на русском) и опиши её одним предложением.

Верни ТОЛЬКО JSON, без пояснений и markdown:
{{
  "label": "...",
  "description": "..."
}}

Отзывы:
{reviews_sample}
"""

OUTREACH_DRAFT_PROMPT = """Ты — copywriter, который пишет короткие холодные письма для B2B-аутрича в России.
Адресат — владелец/директор компании, которая работает в нише "{niche}" в городе {city}.

Компания: {company_name}.
Источник лида: автоматический парсинг {source} (2GIS / Я.Карты) — у нас есть отзывы её клиентов.

Из отзывов клиентов этой компании за последние месяцы видны такие повторяющиеся проблемы:
{pains_block}

От лица сервиса — отправителя письма — предложи помощь с одной из этих проблем.
Сервис отправителя НЕ называй (получатель добавит подпись сам). Подпись и контакты НЕ добавляй.
Не упоминай, что у тебя есть «много отзывов» или «парсинг» — это пугает. Просто говори «обратил внимание / заметил, что в публичных отзывах ваших клиентов встречается ...».

Стиль письма:
- На «вы», уважительно, без воды и канцелярита.
- Тема письма короткая, без капса и эмодзи, без скобок.
- Тело — 4-6 коротких абзацев, всего не более 130 слов.
- В первом абзаце — конкретное наблюдение про компанию (по одной из болей + короткая цитата). Цитату приводи как «один из клиентов писал: ...».
- Дальше — одно конкретное предложение, как ты мог бы помочь с этой болью.
- В конце — мягкий призыв ответить или созвониться 15 минут.

Верни ТОЛЬКО JSON, без пояснений и markdown:
{{
  "subject": "...",
  "body": "..."
}}
"""
