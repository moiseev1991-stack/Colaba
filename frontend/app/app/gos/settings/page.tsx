'use client';

export default function GosSettingsPage() {
  return (
    <div className="mx-auto max-w-4xl px-6 py-8">
      <h1 className="text-[20px] font-semibold" style={{ color: 'hsl(var(--text))' }}>Настройки</h1>
      <p className="mt-4 text-[14px]" style={{ color: 'hsl(var(--muted))' }}>Модуль в разработке.</p>
    </div>
  );
}
