# LeadGen Constructor - Статус реализации по ТЗ

**Последнее обновление**: 24 января 2026

## ✅ Реализовано

### 1. Backend API
- ✅ FastAPI приложение с модульной архитектурой
- ✅ Модели БД: Search, SearchResult (с SEO score, контактами, outreach текстами)
- ✅ API роутеры для searches и filters
- ✅ Интеграция с DuckDuckGo (бесплатный, без API ключа) - используется по умолчанию
- ✅ Интеграция с Яндекс XML API (требует ключи, опционально)
- ✅ Интеграция с Яндекс HTML (бесплатный, парсинг обычного поиска)
- ✅ Интеграция с Google HTML (бесплатный, парсинг обычного поиска)
- ✅ Защита от блокировок (ротация User-Agent, прокси, задержки, детектирование капчи)
- ✅ Автоматический fallback между провайдерами
- ✅ Celery задачи для фонового выполнения поиска
- ✅ **Автоматический SEO аудит для каждого домена**
- ✅ **Автоматический мини-краулер (до 20 страниц)**
- ✅ **Извлечение контактов (телефон, email)**
- ✅ **Сохранение SEO score и контактов в SearchResult**
- ✅ **Генерация outreach текстов на основе SEO проблем**
- ✅ **Группировка результатов по доменам**
- ✅ Система blacklist доменов (seed + пользовательские)
- ✅ Временный обход авторизации для MVP тестирования (user_id=1 по умолчанию)
- ✅ Security утилиты (JWT, password hashing) - готовы к использованию

### 2. Frontend
- ✅ Next.js 14 с App Router
- ✅ Компонент SearchForm для создания поисков
- ✅ Страница результатов с отображением найденных сайтов
- ✅ API клиент для взаимодействия с backend
- ✅ Обработка состояний загрузки и ошибок

### 3. DevOps
- ✅ Docker Compose для локальной разработки
- ✅ PostgreSQL 16 с pgvector
- ✅ Redis для кеширования и Celery
- ✅ Celery worker для фоновых задач
- ✅ Автоматический перезапуск при изменениях
- ✅ Health check endpoints (`/health`, `/ready`)

### 4. Cursor Agents
- ✅ Orchestrator Agent - главный координатор
- ✅ 10 специализированных агентов (Architecture, Backend, Frontend, DevOps, QA, Security, Documentation, Code Review, Optimization, Learning)
- ✅ Правила и best practices для каждого агента

## 🔄 Работает

### Текущий функционал:
1. **Создание поиска**: POST `/api/v1/searches` - создает поиск и запускает Celery задачу
2. **Список поисков**: GET `/api/v1/searches` - возвращает все поиски пользователя (сейчас user_id=1)
3. **Детали поиска**: GET `/api/v1/searches/{id}` - информация о поиске
4. **Результаты поиска**: GET `/api/v1/searches/{id}/results` - список найденных сайтов с SEO score и контактами
5. **Группированные результаты**: GET `/api/v1/searches/{id}/results/grouped` - результаты сгруппированы по доменам
6. **SEO аудит**: POST `/api/v1/filters/audit` - аудит URL с детальными правилами
7. **Blacklist**: POST `/api/v1/filters/blacklist` - добавление доменов в черный список

### Celery задачи:
- ✅ `execute_search_task` - выполнение поиска через Яндекс XML API
- ✅ `process_domain_task` - обработка домена (краулинг + аудит + контакты + outreach)
- ✅ Фильтрация blacklisted доменов
- ✅ Сохранение результатов в БД

### Автоматический процесс обработки:
1. Создание поиска → запуск `execute_search_task`
2. Получение результатов от выбранного провайдера (DuckDuckGo/Яндекс XML) → сохранение в БД
3. **Для каждого уникального домена** → запуск `process_domain_task`:
   - Мини-краулер (до 20 страниц)
   - Извлечение контактов (телефон, email)
   - Если контакты найдены → SEO аудит
   - Генерация outreach текстов на основе SEO проблем
   - Обновление всех результатов домена

## ✅ Реализовано (дополнительно)

### 1. Авторизация и аутентификация
- ✅ User модель и таблица в БД
- ✅ Login endpoint (POST `/api/v1/auth/login`)
- ✅ Register endpoint (POST `/api/v1/auth/register`)
- ✅ Refresh token endpoint
- ✅ Полноценная авторизация с JWT токенами
- ✅ Security утилиты (JWT, password hashing с bcrypt)
- ✅ Frontend страницы login/register
- ✅ Сохранение токенов в localStorage
- ✅ Автоматическое добавление токенов в заголовки запросов

### 2. Multi-tenancy
- ✅ Organization модель и таблица в БД
- ✅ `organization_id` в модели Search
- ✅ Фильтрация по `organization_id` в запросах
- ✅ RBAC (Role-Based Access Control) для организаций
- ✅ Роли: OWNER, ADMIN, MEMBER
- ✅ Глобальное администрирование (суперадминистраторы)
- ✅ Frontend для управления организациями
- ✅ API endpoints для управления организациями и пользователями

### 3. AI-ассистенты, обход капчи, настройки провайдеров

- ✅ **AI-ассистенты**  
  - Модель `AiAssistant` (provider_type, model, config, supports_vision, is_default), миграция 005  
  - Реестр провайдеров: openai, anthropic, google, mistral, ollama, groq, together, openrouter, azure_openai, xai, deepseek, other  
  - CRUD API `/api/v1/ai-assistants`, GET `/ai-assistants/registry`  
  - Клиент `chat()` и `vision()` в `app.modules.ai_assistants.client`  
  - Страница `/settings/ai-assistants` (CRUD, шаблоны, «Сделать по умолчанию»)  
- ✅ **Обход капчи**  
  - Модель `CaptchaBypassConfig` (ai_assistant_id, external_services: 2captcha, anticaptcha), миграция 006  
  - API GET/PUT `/api/v1/captcha-config`, POST `test-2captcha`, `test-ai`  
  - Solver: `solve_image_captcha` (AI Vision), `solve_recaptcha` (2captcha, anticaptcha), **`solve_yandex_smartcaptcha`** (2captcha, `method=yandex`) для Yandex SmartCaptcha  
  - `_try_submit_yandex_captcha_form` с `solution` (image) и **`smart_token`** (SmartCaptcha: `smart-token`, `captcha-token`, `g-recaptcha-response`)  
  - Интеграция в `common.detect_blocking`, `yandex_html` (image → при `None` SmartCaptcha через 2captcha), `google_html` (подстановка в формы, retry)  
  - Страница `/settings/captcha`  
- ✅ **Настройки провайдеров поиска**  
  - Модель `SearchProviderConfig` (provider_id, config), миграция 004, реестр `PROVIDER_REGISTRY`  
  - API GET/PUT `/api/v1/providers`, POST `/providers/{id}/test` (тест с `enable_fallback=False`)  
  - `get_provider_config(provider_id, db)` — БД + env (USE_PROXY, PROXY_*, YANDEX_XML_FOLDER_ID/KEY, SERPAPI_KEY)  
  - `get_proxy_config(proxy_overrides)` в `common.py` — прокси из provider_config или env; `fetch_with_retry(referer=..., proxy_overrides=...)` — referer (yandex.ru, google.com), прокси  
  - `use_mobile` для yandex_html — мобильный URL `.../search/touch/?text=...`  
  - DEBUG: при 0 результатов в HTML-провайдерах в лог пишутся первые 5000 символов HTML  
  - Страница `/settings/providers`: проверка токена до load (при отсутствии — «Войдите» + ссылка на login), `getErrorMessage` для 401/403/5xx; бэкенд `get_providers_list` — try/except, обработка `row.config is None`

## ❌ НЕ реализовано (критично для production)

### 3. Интеграция с Яндекс XML API
- ⚠️ Используется mock-данные при отсутствии ключа
- ✅ Полная интеграция с Яндекс XML API реализована
- ✅ Обработка ошибок API
- ⚠️ Rate limiting (нужно добавить для production)

## 📋 Что нужно доработать

### ✅ Приоритет 1: Улучшения краулера (РЕАЛИЗОВАНО)

1. **Обработка ошибок и ретраи**
   - ✅ Ретраи с экспоненциальной задержкой (3 попытки)
   - ✅ Обработка 403/429/500 ошибок
   - ✅ Обработка таймаутов
   - ✅ Детальное логирование ошибок

2. **Кеширование**
   - ✅ Кеширование результатов краулинга в Redis
   - ✅ TTL: 24 часа
   - ✅ Автоматическое использование кеша при повторных запросах

3. **Fallback стратегии**
   - ✅ Минимальный краулинг (только базовый URL) при ошибках
   - ✅ Возврат минимальной структуры данных при полном провале
   - ✅ Graceful degradation

4. **Оптимизация**
   - ✅ Правильный User-Agent заголовок
   - ✅ Ограничения соединений (connection pooling)
   - ✅ Настраиваемые таймауты

### Приоритет 2: Отправка outreach текстов (следующий шаг)

1. **Настройка поисковых провайдеров** (реализовано)
   - **DuckDuckGo** - работает сразу, без настройки (по умолчанию)
   - **Яндекс XML** (Yandex Cloud Search API) — опционально, YANDEX_XML_FOLDER_ID и YANDEX_XML_KEY в .env
   - Rate limiting для production (опционально)

2. **Улучшения краулера**
   - Обработка 403/капчи (через Unlocker)
   - Ретраи и fallback
   - Улучшенная обработка ошибок

3. **Отправка outreach текстов**
   - Интеграция с Telegram/WhatsApp/Email
   - Планирование отправки
   - История отправок

## 🚀 Как использовать (текущее состояние)

### Запуск проекта:
```bash
# Windows
.\scripts\start.ps1

# Или через Docker Compose
docker compose up -d --build
```

**⚠️ Важно:** после изменения `requirements.txt` (например, добавления `duckduckgo-search`) нужно **пересобрать** образы backend и celery-worker, иначе DuckDuckGo не будет работать:
```bash
docker compose up -d --build
# или только backend + worker:
docker compose build backend celery-worker && docker compose up -d
```

### Создание поиска:
```bash
# С DuckDuckGo (по умолчанию, бесплатно, без ключа)
curl -X POST http://localhost:8000/api/v1/searches \
  -H 'Content-Type: application/json' \
  -d '{"query":"ремонт окон Москва","num_results":50}'

# С Яндекс XML API (требует ключи в .env)
curl -X POST http://localhost:8000/api/v1/searches \
  -H 'Content-Type: application/json' \
  -d '{"query":"ремонт окон Москва","num_results":50,"search_provider":"yandex_xml"}'
```

### Проверка результатов:
```bash
# Получить список поисков
curl http://localhost:8000/api/v1/searches

# Получить результаты поиска (с SEO score и контактами)
curl http://localhost:8000/api/v1/searches/{id}/results

# Получить результаты, сгруппированные по доменам
curl http://localhost:8000/api/v1/searches/{id}/results/grouped
```

### Frontend:
- Откройте http://localhost:3000
- Введите запрос и нажмите "Найти"
- Результаты появятся через несколько секунд
- Для каждого домена автоматически запустится краулинг, SEO аудит и генерация outreach текстов

## 📊 Текущий статус сервисов

- ✅ PostgreSQL - работает
- ✅ Redis - работает  
- ✅ Backend API - работает
- ✅ Celery Worker - работает
- ✅ Frontend - работает

## 🔗 URL

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/api/docs
- PostgreSQL: localhost:5432
- Redis: localhost:6379

## 📝 Структура данных SearchResult

```json
{
  "id": 1,
  "search_id": 1,
  "position": 1,
  "title": "Заголовок",
  "url": "https://example.com",
  "snippet": "Описание",
  "domain": "example.com",
  "seo_score": 75,
  "phone": "+7(999)123-45-67",
  "email": "info@example.com",
  "contact_status": "found",
  "outreach_subject": "SEO проблемы на example.com - можно улучшить",
  "outreach_text": "Здравствуйте!\n\nЯ проанализировал ваш сайт...",
  "extra_data": {
    "crawl": {
      "total_pages": 15,
      "pages": [...]
    },
    "audit": {
      "issues": ["no_robots_txt", "empty_meta_description"],
      "details": {...}
    }
  }
}
```

## 🎯 Основные достижения

1. ✅ Полный цикл обработки: поиск → краулинг → аудит → контакты → outreach
2. ✅ Автоматизация всех процессов через Celery
3. ✅ Группировка результатов по доменам
4. ✅ Генерация персонализированных outreach текстов
5. ✅ Фильтрация через blacklist
6. ✅ Рабочий frontend с интеграцией API
7. ✅ Cursor Agents для автоматизации разработки
8. ✅ Интеграция с DuckDuckGo (бесплатный поиск без ключа) - работает сразу!
9. ✅ Интеграция с Яндекс XML API (опционально, требует ключи)

## 📌 Следующие шаги

1. **Реализовать авторизацию** (User модель + login/register endpoints)
2. **Добавить multi-tenancy** (Organization модель + organization_id)
3. **Обновить все endpoints** для использования реальной авторизации
4. **Протестировать поиск** - DuckDuckGo работает сразу, Яндекс XML требует ключи (см. `docs/guides/YANDEX_XML_SETUP.md`)
5. **Улучшить краулер** (обработка ошибок, ретраи)

## 📚 Дополнительная документация

- Поисковые провайдеры: `docs/guides/SEARCH_PROVIDERS.md` ⭐ (DuckDuckGo работает сразу!)
- Настройка провайдеров (страница, API, get_provider_config): `docs/guides/PROVIDERS_SETTINGS.md`
- Настройка Яндекс XML API: `docs/guides/YANDEX_XML_SETUP.md`
- AI-ассистенты (CRUD, реестр, chat/vision): `docs/guides/AI_ASSISTANTS.md`
- Обход капчи (AI Vision, 2captcha, anticaptcha, интеграция в HTML-провайдеры): `docs/guides/CAPTCHA_BYPASS.md`
- Структура проекта: `docs/guides/PROJECT_STRUCTURE_RULES.md`
- Git workflow: `docs/guides/REPOSITORY_WORKFLOW_RULES.md`
- Локальная установка: `docs/guides/LOCAL_SETUP.md`
