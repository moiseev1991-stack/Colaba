# Как проверить работает ли CI/CD

## ✅ Что уже настроено (в коде)

- ✅ Workflow файлы созданы:
  - `.github/workflows/ci.yml` - CI pipeline
  - `.github/workflows/deploy.yml` - Deploy pipeline
- ✅ Скрипт деплоя: `scripts/deployment/deploy.sh`
- ✅ Docker Compose: `docker-compose.prod.yml`

## 🔍 Как проверить работает ли CI/CD

### 1. Проверка CI (автоматически при push)

**Что нужно сделать:**
1. Сделайте любой commit и push в репозиторий:
   ```bash
   git add .
   git commit -m "Test CI"
   git push origin main
   ```

2. Перейдите в GitHub:
   - Откройте репозиторий: `https://github.com/moiseev1991-stack/Colaba`
   - Перейдите в **Actions** (вкладка вверху)
   - Должен запуститься workflow **"CI"**

3. Проверьте результат:
   - ✅ Если видите зеленые галочки - CI работает
   - ❌ Если красные крестики - есть ошибки (смотрите логи)

**Что проверяет CI:**
- Backend: запускает тесты (`pytest`)
- Frontend: линтинг, проверка типов, тесты (`jest`)

---

### 2. Проверка Deploy (только после успешного CI на main)

**Что нужно сделать:**
1. Убедитесь что CI прошел успешно
2. Сделайте merge в ветку `main` (или push напрямую в main)
3. В **Actions** должен запуститься workflow **"Deploy (main)"**

4. Проверьте результат:
   - ✅ Job `build_images` - должен собрать и запушить образы в GHCR
   - ⚠️ Job `deploy` - **будет ждать self-hosted runner** (если runner не настроен - будет висеть в ожидании)

**Если deploy не запускается:**
- Проверьте что вы в ветке `main`
- Проверьте что CI прошел успешно перед этим
- Проверьте что workflow файлы закоммичены в репозиторий

---

### 3. Проверка Self-hosted Runner (на сервере)

**Если deploy висит в ожидании runner:**

1. Проверьте на сервере что runner установлен:
   ```bash
   cd /opt/actions-runner
   sudo ./svc.sh status
   ```

2. Проверьте в GitHub:
   - Перейдите: **Settings → Actions → Runners**
   - Должен быть виден runner со статусом **"Online"** (зеленый)

3. Если runner не виден или offline:
   - Установите runner (см. `CI_CD.md`)
   - Запустите: `sudo ./svc.sh start`

---

### 4. Проверка что все работает (после деплоя)

**На сервере:**

```bash
# Проверка контейнеров
cd /opt/colaba
docker compose -f docker-compose.prod.yml ps

# Должны быть запущены:
# - leadgen-postgres
# - leadgen-redis
# - leadgen-backend
# - leadgen-frontend
# - leadgen-celery-worker

# Проверка health endpoints
curl http://localhost:8000/health  # Backend
curl http://localhost:3000/        # Frontend
```

---

## 📊 Статус CI/CD

### ✅ CI должен работать сразу после push

**Проверка:**
- Сделайте push → откройте Actions → должен запуститься CI

### ⚠️ Deploy требует настройки

**Что нужно для работы Deploy:**

1. **Self-hosted runner на сервере:**
   - Установлен и запущен
   - Виден в GitHub как "Online"

2. **Файл `/opt/colaba/.env` на сервере:**
   - Создан с правильными переменными
   - Содержит SECRET_KEY, пароли и т.д.

3. **Docker на сервере:**
   - Установлен и работает
   - Пользователь `deploy` в группе `docker`

---

## 🚨 Частые проблемы

### Проблема: CI не запускается

**Причины:**
- Workflow файлы не закоммичены в репозиторий
- Файлы находятся не в `.github/workflows/`
- Синтаксические ошибки в YAML

**Решение:**
```bash
# Проверьте что файлы есть
ls .github/workflows/

# Проверьте синтаксис YAML (онлайн валидатор)
# Или просто посмотрите логи в GitHub Actions
```

### Проблема: Deploy висит в ожидании

**Причина:**
- Self-hosted runner не установлен или не запущен

**Решение:**
- Установите runner (см. `CI_CD.md`)
- Проверьте статус: `sudo ./svc.sh status`

### Проблема: Deploy падает с ошибкой

**Причины:**
- Нет файла `/opt/colaba/.env`
- Неправильные переменные в `.env`
- Docker не установлен
- Нет доступа к GHCR

**Решение:**
- Проверьте логи в GitHub Actions
- Проверьте файл `.env` на сервере
- Проверьте что Docker работает

---

## ✅ Быстрая проверка

**Минимальная проверка (без сервера):**

```bash
# 1. Проверьте что workflow файлы есть
ls .github/workflows/ci.yml .github/workflows/deploy.yml

# 2. Сделайте тестовый commit
git add .
git commit -m "Test CI"
git push origin main

# 3. Откройте GitHub → Actions
# Должен запуститься CI workflow
```

**Полная проверка (с сервером):**

1. ✅ CI запускается при push
2. ✅ Deploy запускается после успешного CI на main
3. ✅ Self-hosted runner онлайн
4. ✅ Контейнеры запущены на сервере
5. ✅ Health endpoints отвечают

---

## 📝 Итого

**CI/CD настроен в коде:** ✅ Да

**CI работает:** Проверьте сделав push и открыв Actions

**Deploy работает:** Требует настройки self-hosted runner на сервере

**Для полной работы нужно:**
1. Self-hosted runner на сервере
2. Файл `/opt/colaba/.env` на сервере
3. Docker на сервере
