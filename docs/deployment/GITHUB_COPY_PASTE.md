# GitHub Secrets и Variables - Скопировать и Вставить

## 🔐 SECRETS (Repository secrets)

**Нажмите "New repository secret" и добавьте:**

### ❌ НИЧЕГО НЕ ДОБАВЛЯТЬ!

В текущей конфигурации используются встроенные токены GitHub Actions, поэтому Secrets не требуются.

---

## 📋 VARIABLES (Repository variables)

**Переключитесь на вкладку "Variables", нажмите "New repository variable" и добавьте:**

### ⚠️ ОПЦИОНАЛЬНО (можно не добавлять, если порты по умолчанию подходят)

#### Переменная 1:
```
Name: BACKEND_PORT
Value: 8000
```

#### Переменная 2:
```
Name: FRONTEND_PORT
Value: 3000
```

---

## ✅ ИТОГО

**Secrets:** Ничего не добавлять ✅

**Variables:** Можно добавить 2 переменные выше (опционально) ⚠️

---

## 🖥️ ВАЖНО!

**Все пароли и ключи хранятся НЕ в GitHub, а в файле `/opt/colaba/.env` на сервере!**

См. файл `COPY_PASTE_VALUES.md` для значений сервера.
