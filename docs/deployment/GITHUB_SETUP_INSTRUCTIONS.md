# Инструкция: Что вставить в GitHub Secrets и Variables

## 🔐 Вкладка "Secrets"

**НИЧЕГО НЕ НУЖНО ДОБАВЛЯТЬ!**

В текущей конфигурации используются встроенные токены GitHub Actions (`GITHUB_TOKEN`), поэтому дополнительные Secrets не требуются.

Просто закройте эту вкладку - ничего делать не нужно.

---

## 📋 Вкладка "Variables"

**ОПЦИОНАЛЬНО** - можно добавить переменные для переопределения портов (если нужно).

### Если хотите добавить переменные:

1. Переключитесь на вкладку **"Variables"**
2. Нажмите **"New repository variable"**
3. Добавьте (если нужно):

#### Переменная 1:
- **Name:** `BACKEND_PORT`
- **Value:** `8000`

#### Переменная 2:
- **Name:** `FRONTEND_PORT`
- **Value:** `3000`

**Примечание:** Эти переменные опциональны. Если не добавлять, будут использоваться значения по умолчанию из `docker-compose.prod.yml`.

---

## ✅ Итого

### Secrets:
- ❌ **Ничего не добавлять** - используются встроенные токены

### Variables:
- ⚠️ **Опционально** - можно добавить `BACKEND_PORT` и `FRONTEND_PORT` (если нужно переопределить порты)

---

## 🖥️ Важно!

**Все секретные данные (пароли, ключи) хранятся НЕ в GitHub, а в файле `/opt/colaba/.env` на сервере!**

См. файл `COPY_PASTE_VALUES.md` для значений, которые нужно добавить в `.env` на сервере.

---

## 📝 Краткая инструкция

1. **Secrets:** Ничего не делать ✅
2. **Variables:** Можно добавить порты (опционально) ⚠️
3. **Сервер:** Создать файл `/opt/colaba/.env` с секретами (обязательно!) 🔐

Готово! 🎉
