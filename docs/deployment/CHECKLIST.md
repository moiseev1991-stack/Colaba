# Чеклист настройки CI/CD

Используйте этот чеклист для проверки правильности настройки CI/CD.

## ✅ GitHub репозиторий

- [ ] Репозиторий создан на GitHub: `https://github.com/moiseev1991-stack/Colaba`
- [ ] Workflow файлы находятся в `.github/workflows/`:
  - [ ] `ci.yml` - существует и корректен
  - [ ] `deploy.yml` - существует и корректен
- [ ] Файлы проекта закоммичены и запушены в репозиторий

## ✅ Self-hosted Runner

### На сервере:

- [ ] Docker установлен и работает (`docker --version`)
- [ ] Docker Compose установлен (`docker compose version`)
- [ ] Пользователь `deploy` создан
- [ ] Пользователь `deploy` добавлен в группу `docker`
- [ ] Директория `/opt/colaba` создана и принадлежит пользователю `deploy`
- [ ] Self-hosted runner установлен в `/opt/actions-runner`
- [ ] Self-hosted runner запущен как systemd service
- [ ] Runner виден в GitHub: **Settings → Actions → Runners** (статус: Online)

### Проверка runner:

```bash
# Проверка статуса
cd /opt/actions-runner
sudo ./svc.sh status

# Проверка логов
sudo journalctl -u actions.runner.* -f
```

## ✅ Файл окружения

- [ ] Файл `/opt/colaba/.env` создан
- [ ] Файл содержит все обязательные переменные:
  - [ ] `SECRET_KEY` - заполнен надежным значением
  - [ ] `POSTGRES_USER` - заполнен
  - [ ] `POSTGRES_PASSWORD` - заполнен надежным паролем
  - [ ] `POSTGRES_DB` - заполнен
  - [ ] `DATABASE_URL` - заполнен корректно
  - [ ] `DATABASE_URL_SYNC` - заполнен корректно
  - [ ] `REDIS_URL` - заполнен
  - [ ] `CELERY_BROKER_URL` - заполнен
  - [ ] `CELERY_RESULT_BACKEND` - заполнен
  - [ ] `NEXT_PUBLIC_API_URL` - заполнен URL вашего домена
  - [ ] `CORS_ORIGINS` - заполнен вашим доменом
- [ ] Файл `.env` имеет правильные права доступа (только для пользователя deploy)
- [ ] Файл `.env` НЕ коммитится в репозиторий (проверьте `.gitignore`)

### Проверка прав доступа:

```bash
ls -la /opt/colaba/.env
# Должно быть: -rw------- 1 deploy deploy
```

## ✅ Docker Compose файл

- [ ] Файл `docker-compose.prod.yml` существует в корне репозитория
- [ ] Файл использует переменные окружения из `.env`
- [ ] Образы указаны через переменные: `${BACKEND_IMAGE}` и `${FRONTEND_IMAGE}`

## ✅ Скрипт деплоя

- [ ] Файл `scripts/deployment/deploy.sh` существует
- [ ] Скрипт имеет права на выполнение (`chmod +x`)
- [ ] Скрипт корректно обрабатывает переменные окружения

## ✅ Тестирование CI

- [ ] Создан тестовый commit и push в любую ветку
- [ ] Workflow `CI` запустился автоматически
- [ ] Job `Backend tests` успешно выполнился
- [ ] Job `Frontend checks` успешно выполнился
- [ ] Все тесты прошли без ошибок

### Проверка в GitHub:

1. Перейдите в **Actions** в репозитории
2. Найдите последний запуск workflow `CI`
3. Убедитесь, что оба job завершились успешно (зеленые галочки)

## ✅ Тестирование Deploy

- [ ] Сделан merge в ветку `main` после успешного CI
- [ ] Workflow `Deploy (main)` запустился автоматически
- [ ] Job `build_images` успешно собрал и запушил образы в GHCR
- [ ] Job `deploy` успешно выполнился на self-hosted runner
- [ ] Контейнеры запущены и работают на сервере

### Проверка на сервере:

```bash
cd /opt/colaba
docker compose -f docker-compose.prod.yml ps
# Все контейнеры должны быть в статусе "Up"
```

### Проверка health endpoints:

```bash
# Backend
curl http://localhost:8000/health

# Frontend
curl http://localhost:3000/
```

### Проверка образов в GHCR:

1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **Packages**
3. Убедитесь, что видны образы:
   - `colaba-backend`
   - `colaba-frontend`

## ✅ Безопасность

- [ ] Файл `.env` не коммитится в репозиторий
- [ ] `.env` добавлен в `.gitignore`
- [ ] Используются надежные пароли
- [ ] `SECRET_KEY` сгенерирован случайным образом
- [ ] Права доступа к файлам настроены правильно
- [ ] Self-hosted runner имеет ограниченный доступ

## ✅ Мониторинг и логи

- [ ] Знаете, где находятся логи контейнеров:
  ```bash
  docker logs leadgen-backend
  docker logs leadgen-frontend
  docker logs leadgen-postgres
  docker logs leadgen-redis
  docker logs leadgen-celery-worker
  ```
- [ ] Знаете, как проверить статус runner:
  ```bash
  cd /opt/actions-runner
  sudo ./svc.sh status
  ```
- [ ] Знаете, где смотреть логи GitHub Actions (вкладка **Actions** в репозитории)

## 📝 Дополнительные проверки

- [ ] Миграции Alembic выполняются корректно при деплое
- [ ] Health checks проходят успешно
- [ ] Все сервисы доступны после деплоя
- [ ] Celery worker запускается и работает
- [ ] База данных сохраняет данные между деплоями (volumes настроены)

## 🆘 Если что-то не работает

1. Проверьте логи в GitHub Actions
2. Проверьте логи на сервере:
   ```bash
   cd /opt/colaba
   docker compose -f docker-compose.prod.yml logs
   ```
3. Проверьте статус runner:
   ```bash
   cd /opt/actions-runner
   sudo ./svc.sh status
   sudo journalctl -u actions.runner.* -f
   ```
4. Проверьте файл `.env` на наличие всех необходимых переменных
5. Проверьте, что образы успешно собраны и доступны в GHCR

## 📚 Документация

- [ ] Прочитана документация: `CI_CD.md`
- [ ] Прочитана документация: `GITHUB_ACTIONS_SETUP.md`
- [ ] Изучен пример файла: `ENV_PRODUCTION.example`
- [ ] Просмотрен быстрый старт: `QUICK_START.md`
